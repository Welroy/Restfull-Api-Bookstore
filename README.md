# Restfull-Api-Bookstore

Build the image using the following command

$ docker build -t book3 .

Run the Docker container using the command shown below.

$ docker run -d -p 4040:4040 --name store3 book3
